name: Build ARMGDDN Downloader

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            platform: linux
          - os: windows-latest
            platform: win32
          - os: macos-latest
            platform: mac

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: false

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Set app version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          node -e "const fs=require('fs'); const tag=process.env.GITHUB_REF_NAME||''; const v=tag.replace(/^v/,''); if(!/^\d+\.\d+\.\d+/.test(v)) { console.error('Invalid tag version:', tag); process.exit(1);} const files=['package.json','package-lock.json']; for(const f of files){ if(!fs.existsSync(f)) continue; const j=JSON.parse(fs.readFileSync(f,'utf8')); j.version=v; if(j.packages && j.packages[''] && typeof j.packages[''] === 'object'){ j.packages[''].version=v; } fs.writeFileSync(f, JSON.stringify(j, null, 2)+'\n','utf8'); } console.log('Set version to', v);"

      - name: Download bundled help video (GitHub Release asset)
        shell: bash
        run: |
          mkdir -p assets
          curl -L --fail -o assets/newmultipartzip.mp4 https://github.com/Nildyanna/armgddn-downloader/releases/download/assets-v1/newmultipartzip.mp4

      - name: Download rclone (Linux)
        if: matrix.platform == 'linux'
        run: |
          mkdir -p rclone/linux
          wget -q https://downloads.rclone.org/rclone-current-linux-amd64.zip
          unzip -q rclone-current-linux-amd64.zip
          mv rclone-*-linux-amd64/rclone rclone/linux/rclone
          chmod +x rclone/linux/rclone
          rm -rf rclone-*-linux-amd64*

      - name: Download rclone (Windows)
        if: matrix.platform == 'win32'
        shell: bash
        run: |
          mkdir -p rclone/win32
          curl -L -o rclone.zip https://downloads.rclone.org/rclone-current-windows-amd64.zip
          unzip -q rclone.zip
          mv rclone-*-windows-amd64/rclone.exe rclone/win32/rclone.exe
          rm -rf rclone-*-windows-amd64 rclone.zip

      - name: Download rclone (macOS)
        if: matrix.platform == 'mac'
        run: |
          mkdir -p rclone/darwin
          curl -L -o rclone.zip https://downloads.rclone.org/rclone-current-osx-amd64.zip
          unzip -q rclone.zip
          mv rclone-*-osx-amd64/rclone rclone/darwin/rclone
          chmod +x rclone/darwin/rclone
          rm -rf rclone-*-osx-amd64 rclone.zip

      - name: Generate macOS icon.icns
        if: matrix.platform == 'mac'
        shell: bash
        run: |
          mkdir -p build/icon.iconset
          sips -z 16 16     assets/icon.png --out build/icon.iconset/icon_16x16.png
          sips -z 32 32     assets/icon.png --out build/icon.iconset/icon_16x16@2x.png
          sips -z 32 32     assets/icon.png --out build/icon.iconset/icon_32x32.png
          sips -z 64 64     assets/icon.png --out build/icon.iconset/icon_32x32@2x.png
          sips -z 128 128   assets/icon.png --out build/icon.iconset/icon_128x128.png
          sips -z 256 256   assets/icon.png --out build/icon.iconset/icon_128x128@2x.png
          sips -z 256 256   assets/icon.png --out build/icon.iconset/icon_256x256.png
          sips -z 512 512   assets/icon.png --out build/icon.iconset/icon_256x256@2x.png
          sips -z 512 512   assets/icon.png --out build/icon.iconset/icon_512x512.png
          sips -z 1024 1024 assets/icon.png --out build/icon.iconset/icon_512x512@2x.png
          iconutil -c icns build/icon.iconset -o build/icon.icns
          rm -rf build/icon.iconset

      - name: Build Electron app (Linux)
        if: matrix.platform == 'linux'
        run: npm run build:linux -- --publish never

      - name: Build Electron app (Windows)
        if: matrix.platform == 'win32'
        run: npm run build:win -- --publish never

      - name: Build Electron app (macOS)
        if: matrix.platform == 'mac'
        run: npm run build:mac -- --publish never

      - name: Upload artifacts (Linux)
        if: matrix.platform == 'linux'
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: |
            dist/*.AppImage
            dist/*.deb

      - name: Upload artifacts (Windows)
        if: matrix.platform == 'win32'
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: |
            dist/*.exe

      - name: Upload artifacts (macOS)
        if: matrix.platform == 'mac'
        uses: actions/upload-artifact@v4
        with:
          name: mac-build
          path: |
            dist/*.pkg

  mobile-android:
    permissions:
      contents: read
    runs-on: ubuntu-22.04
    env:
      GRADLE_USER_HOME: ${{ github.workspace }}/.gradle-home

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install Android SDK
        uses: android-actions/setup-android@v3

      - name: Install mobile dependencies
        working-directory: mobile
        run: npm ci --no-audit --no-fund

      - name: Set mobile app version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        working-directory: mobile
        run: |
          node -e "const fs=require('fs'); const tag=process.env.GITHUB_REF_NAME||''; const v=tag.replace(/^v/,''); if(!/^\d+\.\d+\.\d+/.test(v)) { console.error('Invalid tag version:', tag); process.exit(1);} const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); pkg.version=v; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2)+'\n','utf8'); const app=JSON.parse(fs.readFileSync('app.json','utf8')); if(app.expo && typeof app.expo==='object'){ app.expo.version=v; } fs.writeFileSync('app.json', JSON.stringify(app, null, 2)+'\n','utf8'); console.log('Set mobile version to', v);"

      - name: Prebuild Android project
        working-directory: mobile
        run: npx expo prebuild --platform android --non-interactive --clean

      - name: Ensure Android splash color resource exists
        shell: bash
        working-directory: mobile/android/app/src/main/res/values
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path

          path = Path('colors.xml')
          content = path.read_text(encoding='utf-8') if path.exists() else '<?xml version="1.0" encoding="utf-8"?>\n<resources>\n</resources>\n'
          if 'splashscreen_background' not in content:
              if '</resources>' in content:
                  content = content.replace(
                      '</resources>',
                      '  <color name="splashscreen_background">#020617</color>\n</resources>'
                  )
              else:
                  content = '<?xml version="1.0" encoding="utf-8"?>\n<resources>\n  <color name="splashscreen_background">#020617</color>\n</resources>\n'
          path.write_text(content, encoding='utf-8')
          PY

      - name: Increase Gradle heap for Android build
        shell: bash
        working-directory: mobile/android
        run: |
          set -euo pipefail
          if [ -f gradle.properties ]; then
            if grep -q '^org\.gradle\.jvmargs=' gradle.properties; then
              sed -i 's/^org\.gradle\.jvmargs=.*/org.gradle.jvmargs=-Xmx6g -Dfile.encoding=UTF-8/' gradle.properties
            else
              printf '\norg.gradle.jvmargs=-Xmx6g -Dfile.encoding=UTF-8\n' >> gradle.properties
            fi
          else
            printf 'org.gradle.jvmargs=-Xmx6g -Dfile.encoding=UTF-8\n' > gradle.properties
          fi

      - name: Build Android debug APK
        working-directory: mobile/android
        run: ./gradlew clean assembleDebug --refresh-dependencies --stacktrace

      - name: Stage mobile APK artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p mobile-artifacts
          cp mobile/android/app/build/outputs/apk/debug/app-debug.apk "mobile-artifacts/ARMGDDN-Companion-Mobile-${GITHUB_REF_NAME:-local}.apk"

      - name: Upload mobile artifact
        uses: actions/upload-artifact@v4
        with:
          name: mobile-android-build
          path: |
            mobile-artifacts/*.apk

  release:
    needs:
      - build
      - mobile-android
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download Linux artifacts
        uses: actions/download-artifact@v4
        with:
          name: linux-build
          path: ./artifacts

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: windows-build
          path: ./artifacts

      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          name: mac-build
          path: ./artifacts

      - name: Download mobile Android artifacts
        uses: actions/download-artifact@v4
        with:
          name: mobile-android-build
          path: ./mobile-artifacts

      - name: List artifacts
        run: find ./artifacts -type f -ls

      - name: List mobile artifacts
        run: find ./mobile-artifacts -type f -ls

      - name: Sign release artifacts (Ed25519)
        shell: bash
        env:
          UPDATE_ED25519_PRIVKEY_PEM: ${{ secrets.UPDATE_ED25519_PRIVKEY_PEM }}
        run: |
          set -euo pipefail

          if [ -z "${UPDATE_ED25519_PRIVKEY_PEM:-}" ]; then
            echo "Missing required secret UPDATE_ED25519_PRIVKEY_PEM" >&2
            exit 1
          fi

          KEY_PATH="${RUNNER_TEMP}/update-ed25519-priv.pem"
          printf "%s" "${UPDATE_ED25519_PRIVKEY_PEM}" > "${KEY_PATH}"
          chmod 600 "${KEY_PATH}"

          shopt -s nullglob
          for f in ./artifacts/*.deb ./artifacts/*.AppImage ./artifacts/*.exe ./artifacts/*.pkg; do
            [ -f "$f" ] || continue
            echo "Signing $f"
            openssl pkeyutl -sign -inkey "${KEY_PATH}" -rawin -in "$f" -out "${f}.sig.bin"
            base64 -w0 "${f}.sig.bin" > "${f}.sig"
            rm -f "${f}.sig.bin"
          done

      - name: Create release README.txt
        shell: bash
        run: |
          cat > ./artifacts/README.txt <<'EOF'
          ARMGDDN Downloader ${{ github.ref_name }}

          Installation

          Windows:
          - Download the .exe installer
          - Run the installer

          macOS:
          - Download the .pkg
          - Run the installer
          - If the app doesn't work after install, run Fix.command:
            1) Open Fix.command
            2) If macOS blocks it, go to System Settings -> Privacy & Security and allow Fix.command
            3) Run it and enter your password in Terminal when prompted
          - To avoid security prompts for end users, the app must be signed with an Apple Developer ID certificate and notarized.

          Linux (Debian/Ubuntu):
          - Download .deb file
          - Install: sudo dpkg -i armgddn-downloader_*.deb

          Linux (Universal):
          - Download .AppImage file
          - Make executable: chmod +x ARMGDDN*.AppImage
          - Run: ./ARMGDDN*.AppImage

          Mobile (Android):
          - Download the APK from the mobile artifacts
          - Allow installs from unknown sources on your device
          - Install the APK directly
          EOF

      - name: Stage Fix.command for release
        shell: bash
        run: |
          cp ./build/Fix.command ./artifacts/Fix.command

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: "ARMGDDN Downloader ${{ github.ref_name }}"
          body: |
            ## ARMGDDN Downloader ${{ github.ref_name }}
            
            ### Installation
            
            **Windows:**
            - Download the `.exe` installer
            - Run the installer

            **macOS:**
            - Download the `.pkg`
            - Run the installer
            - If the app doesn't work after install, run `Fix.command`:
              1) Open `Fix.command`
              2) If macOS blocks it, go to `System Settings` -> `Privacy & Security` and allow `Fix.command`
              3) Run it and enter your password in Terminal when prompted
            - To avoid security prompts for end users, the app must be signed with an Apple Developer ID certificate and notarized.
            
            **Linux (Debian/Ubuntu):**
            - Download `.deb` file
            - Install: `sudo dpkg -i armgddn-downloader_*.deb`
            
            **Linux (Universal):**
            - Download `.AppImage` file
            - Make executable: `chmod +x ARMGDDN*.AppImage`
            - Run: `./ARMGDDN*.AppImage`

            **Mobile (Android):**
            - Download the APK from the mobile release assets
            - Allow installs from unknown sources on your device
            - Install the APK directly
          files: |
            artifacts/README.txt
            artifacts/Fix.command
            artifacts/*.deb
            artifacts/*.AppImage
            artifacts/*.exe
            artifacts/*.pkg
            artifacts/*.sig
            mobile-artifacts/*.apk
          draft: false
          prerelease: false
